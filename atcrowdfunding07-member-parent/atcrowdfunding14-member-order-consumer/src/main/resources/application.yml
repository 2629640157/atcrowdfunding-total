server:
  port: 6000
eureka:
  client:
    service-url:
     defaultZone: http://localhost:1000/eureka
spring:
  application:
    name: atguigu-crowd-order
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  redis:
    host: 127.0.0.1
  session:
    store-type: redis
